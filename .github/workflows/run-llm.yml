name: Run LLM

on:
  workflow_dispatch:
    inputs:
      max_docs:
        description: 'Maximum number of documents to process per LLM step'
        required: false
        default: '100'
        type: number

permissions:
  contents: write

jobs:
  run-llm:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v4
        with:
          enable-cache: true
          cache-dependency-glob: '**/pyproject.toml'

      - name: Install Python dependencies
        run: uv sync

      - name: Run LLM analysis
        run: uv run python llm_analysis/run.py --max-count ${{ inputs.max_docs }}
        env:
          OPENROUTER_KEY: ${{ secrets.OPENROUTER_KEY }}

      - name: Commit and push LLM outputs
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add llm_analysis/data/sir_summaries.csv \
                  llm_analysis/data/sir_violation_levels.csv \
                  llm_analysis/data/staffing_summaries.csv || true

          if git diff --cached --quiet; then
            echo "No LLM output changes to commit."
            exit 0
          fi

          git commit -m "Update SIR summaries, violation levels, and staffing summaries"
          git push origin "HEAD:${GITHUB_REF_NAME}"
