[build]
  # Run the build script
  base = "website"
  command = "bash build.sh"
  publish = "../dist"
  
[build.environment]
  # Python version
  PYTHON_VERSION = "3.11"
  # Node.js version
  NODE_VERSION = "24"
  
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
